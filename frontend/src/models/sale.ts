export type Sale = {
    id: number; 
    sellerName: string;
    date: string;
    visited: number; 
    deals:number; 
    amount: number;
}

/**aqui irei colocar as definições dos tipos do meu negocio, por exemplo a venda */
